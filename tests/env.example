# WordPress Test Environment Configuration
# Copy this file to .env and update the values for your test environment

# WordPress Base URL (required for e2e tests)
WP_BASE_URL=https://wordpress.local

# WordPress Admin Credentials (required for e2e tests)
WP_ADMIN_USER=michaelamici
WP_ADMIN_PASSWORD=password

# Test Configuration
CI=false
HEADLESS=true
SLOW_MO=0

# Database Configuration (if using custom test database)
DB_NAME=wordpress_dev
DB_USER=wordpress
DB_PASSWORD=wordpress123
DB_HOST=localhost

# PHPUnit Configuration
WP_TESTS_DIR=/tmp/wordpress-tests-lib
WP_TESTS_DOMAIN=wordpress.local
WP_TESTS_EMAIL=monkeycobbler@gmail.com
WP_TESTS_TITLE=WordPress Dev
WP_PHP_BINARY=/usr/bin/php

# Coverage Configuration
COVERAGE_DIR=coverage
COVERAGE_THRESHOLD=80